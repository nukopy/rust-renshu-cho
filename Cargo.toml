[workspace]
resolver = "2"
members = [
    "calc",
    "cat",
    "echo",
    "head",
    "hello",
    "tests/utils",
    "uniq",
    "wc",
]

[workspace.dependencies]
test_util = { package = "utils", path = "tests/utils" }
